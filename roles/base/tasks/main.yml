---
- name: change hostname
  become: true
  hostname:
    name: "{{ hostname }}"

- name: add /etc/hosts
  become: true
  lineinfile:
    path: /etc/hosts
    regexp: '^127\.0\.0\.1.*'
    line: "127.0.0.1 localhost {{ hostname }}"
